# Docs / assets

Drop here for README polish:

- `screenshot.png` — full Control Center UI
- `proxy-status.gif` — sidebar with ● Proxy live / ○ Proxy offline

Then reference in the main README with `docs/screenshot.png` and `docs/proxy-status.gif`.
