# Optional local overrides. Copy to .env and edit if needed.
# Token is read from ~/Documents/permissions/ARC_API_KEYS.txt (GOSCREEN/GOLOIGN) by default.

# Port for the backend (default 8000)
# PORT=8000
